---
credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: slack-token
              secret: "${SLACK_TOKEN}"
              description: Slack token

unclassified:
  slackNotifier:
    teamDomain: "${SLACK_WORKSPACE}"
    tokenCredentialId: slack-token
